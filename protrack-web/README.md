# Moved

The desktop ProTrack app now lives in its own folder next to this repo:

**`/Users/a91788/Desktop/protrack-desktop`**

Add that folder to your Cursor workspace if you want it in the sidebar. Run the API from `pm-pro/backend` as before; run `npm run dev` from `protrack-desktop`.
